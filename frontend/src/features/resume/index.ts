/**
 * Resume — public entrypoint for the feature slice.
 *
 * External consumers import from here; individual components stay private.
 */
export { ResumeSection } from "./components/resume-section";
