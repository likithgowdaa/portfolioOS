/**
 * Landing experience — public entrypoint for the feature slice.
 *
 * External consumers import from here; individual components stay private
 * (except where a lazy chunk requires a direct import).
 */
export { Hero } from "./components/hero";
export { Navbar } from "./components/navbar";
export { OpeningExperience } from "./components/opening-experience";
