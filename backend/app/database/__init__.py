"""Database access layer.

Connections and repositories live here. The Supabase (Postgres) client and
session handling are wired in Sprint 02; the required configuration variables
are already defined in ``app.core.config`` (``SUPABASE_URL``,
``SUPABASE_ANON_KEY``, ``DATABASE_URL``).
"""
